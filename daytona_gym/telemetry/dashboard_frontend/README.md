# Dashboard frontend (Svelte 5 + Vite)

Modal-shaped SPA for `dg dash`.

```bash
cd daytona_gym/telemetry/dashboard_frontend
npm install
npm run build
```

Build output lands in `../dashboard_static/` and is served by `dashboard.py`
(no Node required on the GPU box).
